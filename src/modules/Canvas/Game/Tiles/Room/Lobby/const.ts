import { createRoomTile } from '../../utils';

export const LOBBY = {
  lobby_top_left: createRoomTile({ spriteIndex: 1 }),
  lobby_top_center: createRoomTile({ spriteIndex: 2 }),
  lobby_top_right: createRoomTile({ spriteIndex: 3 }),
  lobby_bottom_left: createRoomTile({ spriteIndex: 4 }),
  lobby_bottom_center: createRoomTile({ spriteIndex: 5 }),
  lobby_bottom_right: createRoomTile({ spriteIndex: 6 }),
};
