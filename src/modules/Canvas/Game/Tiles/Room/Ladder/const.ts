import { createRoomTile } from '../../utils';

export const LADDER = {
  ladder_top_left: createRoomTile({ spriteIndex: 12 }),
  ladder_top_right: createRoomTile({ spriteIndex: 13 }),
  ladder_bottom_left: createRoomTile({ spriteIndex: 14 }),
  ladder_bottom_right: createRoomTile({ spriteIndex: 15 }),
};
